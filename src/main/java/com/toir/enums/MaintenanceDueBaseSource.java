package com.toir.enums;

/** Where the calendar-trigger base date came from, used to build the {@code baseSource} field. */
public enum MaintenanceDueBaseSource {
    COMPLETION_ANCHOR,
    OPERATION_START,
    REGULATION_CREATED,
    CALENDAR_BASE
}
