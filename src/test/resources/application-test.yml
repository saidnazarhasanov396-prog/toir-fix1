spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/postgres
    username: postgres
    password: Sardorbek.01
    hikari:
      connection-timeout: 3000

  flyway:
    enabled: false

  sql:
    init:
      mode: never

  jpa:
    hibernate:
      ddl-auto: create-drop

  test:
    database:
      replace: none