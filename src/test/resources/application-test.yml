spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/toir_demo
    username: postgres
    password: root123
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 3000

  flyway:
    enabled: false

  security:
    jwt:
      secret: test-only-lifecycle-approval-jwt-secret-0123456789abcdef0123456789abcdef

  sql:
    init:
      mode: never

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none

  test:
    database:
      replace: none

minio:
  endpoint: http://minio.test:9000
  access-key: test-access
  secret-key: test-secret
  bucket-name: test-bucket
  public-url:

app:
  firebase:
    enabled: false