spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/toir_demo
    username: postgres
    password: Sardorbek.01
    hikari:
      connection-timeout: 3000

  flyway:
    enabled: false

  sql:
    init:
      mode: never

  jpa:
    hibernate:
      ddl-auto: create-drop

  test:
    database:
      replace: none

minio:
  endpoint: http://minio.test:9000
  access-key: test-access
  secret-key: test-secret
  bucket-name: test-bucket
  public-url:

app:
  firebase:
    enabled: false
